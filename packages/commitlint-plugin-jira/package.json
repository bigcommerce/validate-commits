{
  "name": "@bigcommerce/commitlint-plugin-jira",
  "version": "1.0.0",
  "description": "BigCommerce commitlint plugin for JIRA style commit messages.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.json"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/types": "^12.1.4"
  },
  "peerDependencies": {
    "@commitlint/lint": ">=7.6.0"
  }
}
