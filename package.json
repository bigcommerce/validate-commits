{
  "name": "@bigcommerce/commitlint",
  "version": "1.0.0",
  "description": "Commit validation using commitlint",
  "private": true,
  "author": "BigCommerce",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chanceaclark/validate-commits.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "eslint . --ext .js,.ts",
    "test": "lerna run test --stream"
  },
  "engines": {
    "node": "^14.0.0"
  },
  "prettier": "@bigcommerce/eslint-config/prettier",
  "devDependencies": {
    "@bigcommerce/eslint-config": "next",
    "eslint": "^7.29.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "typescript": "^4.3.4"
  }
}
